services:
    frontend-app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3001:3000'
            - '24678:24678' # to allow Vite to perform hot module reloading
        volumes:
            - .:/app
        environment:
            NODE_ENV: development
